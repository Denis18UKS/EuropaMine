# Navigation terminal build payload

These chunks are assembled by GitHub Actions into the navigation-terminal source overlay before Forge compilation. They are excluded from the published source archive.
